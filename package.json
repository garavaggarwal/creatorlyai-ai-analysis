{
  "name": "creatorly-videolab",
  "version": "1.0.0",
  "description": "AI Video Analysis backend for Creatorly Video Lab",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "postinstall": "mkdir -p /app/bin && (pip3 install -q --target /app/yt-dlp-pkg yt-dlp && ln -sf /app/yt-dlp-pkg/bin/yt-dlp /app/bin/yt-dlp) || (pip install -q --target /app/yt-dlp-pkg yt-dlp && ln -sf /app/yt-dlp-pkg/bin/yt-dlp /app/bin/yt-dlp) || python3 -m pip install -q yt-dlp || true"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "apify-client": "^2.23.3",
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.18.2",
    "ffmpeg-static": "^5.3.0",
    "ffprobe-static": "^3.1.0",
    "fluent-ffmpeg": "^2.1.3",
    "multer": "^1.4.5-lts.1",
    "yt-dlp-wrap": "^2.3.12"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  },
  "engines": {
    "node": ">=18"
  }
}
